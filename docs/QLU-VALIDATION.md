# 齐鲁课表 0.2.7 验证记录

2026-09-21，versionCode 9。E 盘构建及 Android 15 / API 35 专用无窗口模拟器 QluCampusTest35（5562）后台测试。

- 成功构建最终 APK，内置 ZCJ-GIF/QluCampus 的独立更新源。grades 11 项测试通过（新增 5 项网络判定/取消），data 与 update 测试任务通过；七模块报告累计 441 项，未修改模块的历史报告不计为本轮重跑。记录见 build-027-network.log、build-027-release.log。
- 综合 Android UI 1 项通过，涵盖 GPA、成绩锁、课表、日历、桌面组件、更新设置及 aTrust 未安装时的帮助与取消。日志 ui-027-network.log。app/grades Lint 无错误，既有建议和警告保留在报告中。
- 专用模拟器安装明确标注 TEST FIXTURE 的外部客户端，建立真实 Android VPN transport，仅路由保留测试网段 192.0.2.0/24。成绩刷新、空教室选项、课表预览均检测到 VPN 并跳过自动拉起；手动打开/返回与关闭自动开关均通过。测试后替身及其 VPN 已卸载。见 027-vpn-handoff.txt 与 027-*-final.png；不代表真实 aTrust 已认证。
- 最终 APK 包名 com.qlucampus.app、版本 0.2.7/code 9、签名与旧版相同。从 0.2.6 实际 install -r 升级并启动，私有文件标记保留；这不是数据库迁移测试。见 apk-027-verification.txt。
- 数据库仍为 v8。主机 SQL/Room 结构、历史数据与双工作表 XLSX 中文/小数读取检查通过；data Android 39 项为 0.2.0 历史报告。
- GitHub 授权成功，公开仓库 https://github.com/ZCJ-GIF/QluCampus 已创建；发布源配置为 main/version.json，安装包和源码通过 Releases 交付。远程下载核验结果随交付目录中的发布报告提供，不把本地单元测试当作远程下载成功证据。
- Redmi K80 的真实 aTrust 登录/连接、学校原生课表/成绩/空教室结果仍需实体手机核验。Android 不能可靠识别外部 VPN 所有者，因此其他 VPN 也会跳过自动拉起；界面保留手动打开。

交付目录 E:\Android\releases\0.2.7。APK、对应 GPL 源码与许可一并提供；学校密码、Cookie、真实成绩与签名密钥不进入源码或发布仓库。

---

# 齐鲁课表 0.2.6 验证记录（历史）

2026-09-21，versionCode 8。E 盘构建，Android 15 / API 35 专用无窗口模拟器 QluCampusTest35（5562）后台测试，不占用电脑前台。

- 构建成功，grades JVM 6 项通过，其中新增 5 项覆盖首次恢复不误执行、重复点击保留原操作、返回仅消费一次、取消/账号切换清空及下一次查询重新等待。七模块通过报告累计 436 项；其他模块报告沿用之前验证，不计为本轮重跑。
- 综合 Android UI 1 项通过，增加成绩/空教室未安装 aTrust 的帮助、取消后成绩快照保留、自动打开开关保存检查；原有更新设置、GPA、锁定、周课表和桌面组件检查继续通过。
- 外部应用跳转实测使用独立的 `com.sangfor.atrust` 测试替身，界面明确标记 TEST FIXTURE，无 VPN 或网络功能，仅安装于专用模拟器且测试后已卸载。最终 APK 验证成绩刷新、空教室选项加载、官方查询入口、读取课表预览各打开一次，返回继续原操作且不重入；关闭开关后不再打开。不能将替身测试视为真实 aTrust 登录或隧道连接已验证。
- 最终源码已加入课表导入入口，最终构建 `build-026-final.log` 与外部跳转测试 `026-atrust-handoff.txt` 覆盖此追加要求。综合 UI 日志为 `ui-026-vpn.log`，截图为 `026-*-final.png`。测试脚本首次运行的按钮大小写定位错误在替身中修正，没有修改产品逻辑或跳过测试。
- Lint：app 0 错误/11 警告，grades 0/31。新增提示为 SharedPreferences KTX 建议，其余见报告。APK 签名与此前交付一致，包名 com.qlucampus.app、minSdk 26；实际从 0.2.4 install -r 升至 0.2.6，保留私有文件标记并启动成功。该检查不是数据库迁移测试。
- 数据库保持 v8；主机 SQL/Room 结构及双工作表 XLSX 读取检查通过。data Android 39 项为 0.2.0 历史报告，不计入本轮重跑。
- Redmi K80 / 澎湃系统的真实 aTrust 拉起、登录、VPN 连通和学校查询结果尚待手机核验。GitHub 授权仍未完成，仓库和线上发布未创建，更新源暂为空；本轮提供本地 APK 和对应 GPL 源码。

交付目录 `E:\Android\releases\0.2.6`，文件校验值见 `SHA256SUMS.txt`；后台模拟器测试替身不包含在用户安装包或源码包内。

---

# 齐鲁课表 0.2.5 验证记录（发布前）

2026-09-21，versionCode 7。所有构建及 UI 测试在 E 盘和 Android 15 无窗口模拟器 QluCampusTest35（5562）后台执行。

- 更新模块 36 项 JVM 测试通过，包含六小时检查间隔、关闭/空配置不联网、HTTPS 来源规范化、错误应用与缺失元数据拒绝、未知枚举安全回退、TLS、重定向与下载/安装状态策略。其余六模块既有通过报告 395 项，总报告 431 项；未改动的模块未计为本轮重跑。
- 综合 UI 用例 1 项通过，新增设置入口导航、自动开关持久化、HTTP 地址拒绝、空地址停用和恢复来源。继续验证原有课表、GPA、成绩锁、日历和桌面组件。
- UI 首次验收发现旧设置页隐藏更新入口，已补上。禁用按钮测试改为检查 Compose 按钮父节点语义，保留实际禁用断言。TLS 测试固定 127.0.0.1 回环，避免本机 VPN 将 localhost 改写为其他主机名；没有放宽应用证书校验。
- APK v2 签名核验通过，证书与 0.2.4 相同。专用模拟器从 0.2.4 实际 install -r 升至 0.2.5 并启动，私有文件标记保留；该检查不等同于数据库迁移测试。数据库保持 v8，主机迁移 SQL/Room 结构及 XLSX 独立读取检查继续通过。
- 更新模块 Lint 0 错误/4 警告。data Android 39 项报告为 0.2.0 历史记录，不计入本轮重新执行。
- 源码发布目录已排除本机配置、签名密钥、构建缓存及 Git 历史；保留 GPL LICENSE 和上游署名。GitHub 仓库和实际远程更新下载尚待 CLI 授权完成，默认发布地址暂未填入，不能视为联网发布已经完成。
- 用户已反馈找到 Redmi K80 的桌面组件；本轮未对实体手机执行测试。学校原生课表、成绩和空教室结果仍待手机登录核验。

日志：`build-025-update-pass.log`、`ui-025-updates.log`、`apk-025-verification.txt`；截图 `025-update-settings-final.png` 为未配置来源的合成测试状态。

---

# 齐鲁课表 0.2.4 验证记录

2026-09-21，versionCode 6。后台构建及 Android 15 无窗口模拟器 QluCampusTest35（5562）验证，不操作用户前台。

## 本轮范围

- 所查看周次不排课的卡片统一灰色、带“非本周”标注；标准及完整信息两种卡片共用颜色方法，本周原色保留。截图核对普通模式及带壁纸的高对比度模式。
- 总评 60 分为绿字“合格”加淡绿背景，59.5 分为红字“挂科”加淡红背景；“未提供”“优秀”等非数字值保留普通样式。同一成绩组件用于普通与分项页面，不修改原始成绩、GPA 或 Excel。
- 默认作息实际拆为十个 45 分钟小节，白天中间休息 5 分钟、晚课中间休息 0 分钟。主机核对整段时间为 08:30–10:05、10:20–11:55、14:00–15:35、15:50–17:25、18:25–19:55；默认时间轴截图显示对应起止时间。已有自定义作息保留，设置提供恢复五大节；第十一节之后未配置时显示未设置。

## 验证结果

- APK 构建成功。六模块 JVM 测试任务通过（含 up-to-date）：domain 46、data 161、grades 1、timetable 152、widget 6、settings 29，合计 395 项，0 失败/跳过。
- 综合 Android UI 用例 1 项通过，复用原有 GPA、锁定、日历、课表、组件添加/取消/回调与滚动检查，并增加合成颜色预览截图。首次预览运行因额外合成课程改变原调色板计数而失败；将该截图专用课程放在原断言之后插入后通过，不删除或跳过断言。
- Lint：app 0 错误/11 警告，grades 0/30，settings 0/16，timetable 0/27 加 1 提示。widget Lint 报告沿用 0.2.3，未修改 widget 实现。
- 签名验证及从 0.2.3 覆盖安装检查见 `apk-024-verification.txt`；包名、签名不变，升级私有文件标记保留。数据库仍为 v8，无新结构；主机 SQL/Room 与 XLSX 独立读取检查通过。data Android 39 项为 0.2.0 历史报告，未计为本轮重新执行。
- 仅 Android 15 模拟器实测；Redmi K80 桌面组件及学校原生接口待核验状态与 0.2.3 相同。

## 交付

目录 `E:\Android\releases\0.2.4`：APK、对应 GPL 源码、说明、报告及 `024-*-final.png` 合成截图。最终构建日志为 `ui-024-delivery.log`，本轮其余日志保留用于追溯；作息核对为 `024-time-validation.txt`。文件校验值见 `SHA256SUMS.txt`。

---

以下为历史记录，保留各轮实际测试范围。

# 齐鲁课表 0.2.3 验证记录

2026-09-21，versionCode 5。全部构建及测试在 E 盘工程和专用 Android 15 无窗口模拟器 QluCampusTest35（5562）后台执行。

## 本轮结果

- 最终 APK 构建成功。本轮执行 settings、widget 单元测试任务，分别 29、6 项通过（含 up-to-date 任务）；新增 7 项覆盖请求受理与实际成功的区别、回调令牌匹配、迟到回调、桌面拒绝及异常。六模块报告累计 395 项，其他模块报告沿用 0.2.2，不计为本轮重新执行。
- 综合 Android UI 用例 1 项通过，屏幕为 1080×2400、density 420（411dp）。验证组件声明/启用/系统登记检测、手动帮助、取消系统添加后仍显示“尚未确认成功”、重试并实际添加后匹配回调才显示成功；六门合成课程可在桌面组件中滚动，点击可打开应用。同时保留原有 GPA、成绩锁、日历及课表断言。
- 组件新增标准 RemoteViews 预览、初始布局和最小缩放尺寸，保留旧 Android 的 previewImage。原 provider 类名不变。截图使用合成数据，不包含真实账号或成绩。
- Lint：app 0 错误/11 警告，settings 0/24，widget 0/6。settings 新增两项 SharedPreferences KTX 写法建议；widget 新增 previewLayout 的 API 31 提示，旧系统使用保留的 previewImage。其余警告见报告。
- apksigner 验证 v2 签名通过，包名 com.qlucampus.app、versionName 0.2.3、versionCode 5、minSdk 26、targetSdk 36。与 0.2.2 证书相同；模拟器从 0.2.2 实际 install -r 升级至 0.2.3 并启动，私有文件标记保留。该检查不是数据库迁移测试。
- 数据库保持 v8、无结构变更。主机重新检查 v6→v7→v8 SQL/Room 结构和 XLSX 两个工作表的中文、小数及文本内容。data Android 的 39 项报告仍为 0.2.0 历史基线，未计为本轮重跑。

## Redmi K80 待核验

用户报告 Redmi K80、澎湃 3.0.307.0 已进入“安卓小部件”列表，但找不到本应用。原 APK 已包含 provider 注册，当前没有连接该手机，不能据此认定根因，也不能将 Pixel Launcher 测试视为 HyperOS 修复验收。

0.2.3 新增“检测桌面组件”，读取本机的组件声明、启用状态、AppWidgetManager 系统登记、弹窗添加支持和已有组件数量。需在 K80 覆盖安装后读取检测结果，继续区分系统登记缺失与桌面列表展示问题。检测不会修改桌面设置或清除数据；首次请求已受理也不会被当作添加成功。学校原生接口的待验范围沿用 0.2.2，本轮未进行学校联网验收。

## 交付

`E:\Android\releases\0.2.3` 包含测试 APK、对应 GPL 源码、说明、报告及 `023-*-final.png` 截图。构建/UI 日志为 `ui-023-widget.log`，签名及升级日志为 `apk-023-verification.txt`，校验值为 `SHA256SUMS.txt`。

---

以下为历史记录，保留各轮实际测试范围。

# 齐鲁课表 0.2.2 验证记录

2026-09-21，versionCode 4。构建及测试在 E 盘工程、专用 Android 15 无窗口模拟器 QluCampusTest35（5562）后台执行。

## 本轮结果

- 最新 APK 构建成功；六模块 JVM 测试任务通过（包含 up-to-date 任务），共 388 项：domain 46、data 161、grades 1、timetable 152、widget 6、settings 22，0 失败/跳过。
- 综合 Android UI 用例分别在 411dp 和 360dp 屏幕宽度运行通过，各 1 次。411dp 的构建、测试与 Lint 完整日志为 `ui-022-layout.log`；360dp 为 `ui-022-small-screen.log`，最终 XML 分别另存 `022-wide-ui.xml`、`022-small-ui.xml`。窄屏复测中修正了测试脚本对设置页恢复滚动位置的定位方式，未跳过断言。
- 默认七列布局首屏露出第 9、10 节；图层、刷新、管理、添加、导入、设置按钮直接排在日期同行。卡片按屏幕高度分配十节，优先课程名称和地点，教师移至详情；普通与高对比度配色保留。
- 底部导航为 60dp 内容高度加系统安全区，课表页半透明圆角覆盖。截图核对第十节标签仍可见、部分末节处于底栏后；向上滑动可把第 11–12 节与完整地点移出遮挡。150% 字号仍可滚动，极长名称可点击详情。截图均为合成课程，不是用户真实课表。
- 综合 UI 同时覆盖 GPA、平时成绩锁定/解锁和持续开启、首周月份日历、空教室官方入口可点击、桌面组件六门课程滚动及点击。UI 使用合成账号，不作为学校原生接口验收。
- 最新 Lint：app 0 错误/11 警告、grades 0/30、settings 0/22、timetable 0/27 加 1 提示。widget 报告沿用此前基线，未改其实现。
- apksigner 验证 v2 签名通过，包名 `com.qlucampus.app`、versionName 0.2.2、versionCode 4、minSdk 26、targetSdk 36。证书与 0.2.1 相同；专用模拟器实际从 0.2.1 使用 install -r 升至 0.2.2 并启动，私有文件标记保留。此检查不替代数据库迁移测试。
- 数据库保持 v8、无结构变更；本轮重新执行主机 v6→v7→v8 SQL/Room 结构检查及 openpyxl 独立读取 XLSX，中文、小数文本和两个工作表正常。data Android 的 39 项报告沿用 0.2.0，不计为本轮重跑。

## 学校联调范围

用户完成统一认证后，已在学校官方空教室网页验证：2026–2027 学年第 1 学期、菏泽校区、第 3 周星期二（2026-09-22）、第 1–2 节、多媒体教室、全部教学楼，返回 47 条、4 页。已核对网页的周次/星期/节次表格、教学楼/节次级联接口以及学校公开请求脚本的参数和排序规则；新增解析用例通过。

原生空教室适配已按上述证据修正，但 Android 登录后的原生教室结果尚未与官网对照；真实课表、总评、平时成绩也仍待核验。官网查询成功不能替代原生端学校验收。登录/官方页面的统一认证入口为学校首页实际跳转地址；手机需要自行连通学校网络/VPN。

## 交付

`E:\Android\releases\0.2.2` 包含测试 APK、对应 GPL 源码、维护与版本说明、报告及 `022-*-final.png` 截图。可直接覆盖之前同签名版本，无需卸载。APK 签名、升级日志为 `apk-022-verification.txt`，文件校验值见 `SHA256SUMS.txt`。

---

以下为历史记录，保留各轮实际测试范围。

# 齐鲁课表 0.2.1 验证记录

2026-09-21，versionCode 3。本轮全部在 E 盘工程及 Android 15 无窗口模拟器 QluCampusTest35（5562）后台完成。

## 本轮结果

- 构建成功；六模块 JVM 测试任务均通过（含无需重复执行的 up-to-date 任务），共 383 项：domain 46、data 156、grades 1、timetable 152、widget 6、settings 22，0 失败/跳过。
- 综合 Android UI 测试 1 项通过。默认字号下七列周课表显示至第 12 节，合成地址“菏泽校区 菏泽北楼207”完整显示；核对默认、原高对比度、150% 字号与局部毛玻璃截图。放大字体允许滚动，长课程名可点击查看。
- UI 打开“当前学期 → 日期”后显示应用内月份日历、周一选项和确认按钮；新建课表初始日期为空由 ViewModel 测试断言。刷新课表仍使用已保存首周日期。
- 空教室新增 name 控件识别与装饰元素排除测试；综合 UI 检查未成功加载查询选项时官方入口仍可点击。未使用真实学校账号查询教室，不能将此测试理解为原生查询通过学校验收。
- 继续通过 GPA、成绩分项锁定与解锁、持久解锁、手动锁定、桌面组件六门课程滚动与点击测试。
- 本轮 Lint：app 0 错误/11 警告、grades 0/30（依赖版本提示）、settings 0/22、timetable 0/27 加 1 提示。widget 报告沿用 0.2.0；未改其实现。
- APK 签名验证通过，与 0.2.0 同一证书；模拟器实际 install -r 从 0.2.0 升至 0.2.1，私有文件标记保留。该检查不替代数据库迁移测试。
- 数据库仍为 v8、无结构变更；本轮重新执行主机迁移 SQL/Room 结构和 XLSX 独立读取检查。data Android 的 39 项设备报告沿用 0.2.0，未作为本轮重跑计数。
- 用户刷新 aTrust 后匿名学校请求恢复：教务首页 200，空教室候选页面 302 至学校登录。没有当前已登录会话，真实查询条件、参数及教室结果仍未验证。

## 交付及限制

交付目录：`E:\Android\releases\0.2.1`，包含 APK、对应完整源码、说明、报告和截图。默认使用普通配色；高对比度保持 0.2.0 行为，已取消此前未交付的边框配色修改。

官方查询入口是应用内 WebView，与学校登录共用会话，可缩放网页。原生适配未识别页面时明确提示并隐藏不可用按钮；不会用个人课表推算空教室。若学校仍要求登录，应在学校页面完成认证。真实账号与 Android 8.0/实体手机验证仍待完成。

关键日志：`build-021-layout.log`、`ui-021-layout.log`、`lint-021-layout.log`、`apk-021-verification.txt`。截图 `021-*-final.png` 均为合成数据；不含真实成绩、账号或 Cookie。

---

以下为 0.2.0 历史基线记录，保留其测试范围，不能替代本轮结果。

# 齐鲁课表 0.2.0 验证记录

日期：2026-09-21。全部开发验证在后台执行；专用 Android 15 AVD 为 QluCampusTest35，端口 5562，无窗口、无声音。截图和测试数据均为合成示例。

## 已通过

| 项目 | 结果与范围 |
| --- | --- |
| APK | com.qlucampus.app，versionCode 2 / versionName 0.2.0，约 40 MB |
| 系统兼容声明 | minSdk 26（Android 8.0），targetSdk 36；arm64-v8a / armeabi-v7a / x86 / x86_64 |
| JVM 单元测试 | 共 381 项，0 失败/跳过：domain 46、data 154、grades 1、timetable 152、widget 6、settings 22；包含上游已有测试 |
| Android 仪器测试 | data 39 项，应用综合 UI 1 项，0 失败/跳过 |
| Lint | app 0 错误/11 警告；grades 0/17，settings 0/22，timetable 0/27（另 1 提示），widget 0/5；具体条目在报告中 |
| Excel | openpyxl 独立读取两个工作表，中文、小数文本及以等号开头的课程名保留且不作为公式；锁定后持旧快照导出也会去除平时分项 |
| 数据迁移 | 主机执行实际 v6→v7→v8 SQL，对比 Room 字段/主键/外键/索引；设备迁移测试保留历史课程、成绩和导入绑定，验证多份课表与删除级联 |
| 升级安装 | 在专用模拟器先安装/启动 0.1.0，再使用 install -r 安装 0.2.0 并启动 v8 数据库；合成历史记录保留由独立迁移测试覆盖 |
| 签名 | apksigner 验证 v2 RSA-3072；与 0.1.0 相同，可覆盖升级 |
| 桌面小组件 | 通过 Pixel Launcher 添加今日课表，完整列表可滚动到第六门课，点击标题可打开本应用；当天已结束课程仍保留由 timeline 测试断言 |
| 学校匿名入口 | 开发期通过 aTrust，每 5 分钟匿名 GET 教务首页返回 HTTP 200；未更改 Clash 全局配置 |

签名证书 SHA-256：

```text
61e1e5cb9af1decb418a35c81da869bf594c0c0a271a21440253c5c7c60a79a6
```

## 本轮覆盖

- 多课表同账号同学期独立保存、克隆学校绑定；删除最初账号占位课表后其他副本仍可刷新。刷新甲时不切走正在查看的乙，也不改乙的数据；陈旧绑定、修改过的首周日期会拒绝写入。
- 账号/学期缓存隔离、学校会话切换后拒绝迟到写入、缺少 Cookie 时失败保留缓存、事务失败回滚、SQLCipher 冷启动与升级。
- GPA 多个成绩分项只计一门、课程/绩点顺序不同、跨学期选择、零绩点、学分缺失/为零/负数/冲突、绩点缺失/歧义、禁止混账号；界面选中一门 2 学分、3.8 绩点课程得到 3.8000。
- 平时成绩默认隐藏、错误密码拒绝、正确解锁、重建状态仓库仍保持开启、手动锁定后移除主导航入口；普通成绩看不到缓存平时分项，锁定后的 Excel 导出再次过滤旧快照，原详细缓存未删除。
- 150% 字号、长中文课程名、教师/教室完整换行、时间轴宽度自适应；高对比度六门课程不同配色，黑/白文字对比度至少 4.5；毛玻璃卡片、局部模糊与壁纸截图检查。
- “隐藏非本周课程”开关已有，入口为“设置 → 显示与布局”；布局引擎测试覆盖过滤及周末设置，按正在查看周次与单双周判断。
- 空教室日期到教学周映射、无效节次、分页、中文教室名与容量、未知结构、登录页和合法空列表的区别。仅为合成解析测试，不等于学校接口已验证。
- 启动免责声明、作者联系方式与不再显示选项；设置页面入口可再次查看。

## 限制与待验证

- **学校联调待验证**：未输入真实学校账号。实际学号识别、SSO 会话、齐鲁工大课表 JSON/HTML、总评/平时成绩导出字段、空教室候选页面及参数仍需用户在 VPN 可用时完成登录后对照核验。访问登录页成功不能证明这些接口可用。
- 空教室界面已实现查询和适配失败提示；只有确认学校页面存在才提供官方页面回退，不从个人课表推测空教室。未完成学校实测前不能承诺能返回该校教室列表。
- 运行验证为 Android 15 模拟器，尚未在 Android 8.0 镜像或实体手机运行；桌面组件不同厂商启动器的布局/缩放差异仍需真机确认。
- Excel/WPS 未在电脑前台交互打开；通过文件结构与独立读取库检查。
- 测试版使用稳定调试签名；后续本机升级应继续同一包名/签名并递增版本。若正式分发改签名，不能直接覆盖现有测试安装。
- 固定功能密码是本机入口锁，不是学校权限控制；源码公开后不具备防止修改应用绕过的保证。

## 报告与复现

交付目录 `E:\Android\releases\0.2.0` 包含 APK、对应 GPL 源码、维护说明、测试 XML、Lint 文本、构建日志、合成界面截图、独立 XLSX 样例及 SHA-256 清单。

运行命令见 `QLU-MAINTENANCE.md`。先运行 Gradle 单元/设备测试，再执行 `scripts/validate_qlu_artifacts.py`，最后执行 `scripts/package_qlu_release.py`。打包工具排除构建缓存、密钥、local.properties 和用户数据。

UI 自动化的滚动与连续点击等待已调稳，并按包含课程的桌面组件定位，避免测试时误选历史空组件。最终交付以 reports 中最新成功报告为准。
