# iOS 验证记录

版本：0.1.0 (1)，独立 Bundle ID com.qlucampus.ios。2026-09-22 新增原生 SwiftUI 客户端，Android 发布源不变。

已在 GitHub macOS 15 / Xcode 16.4 构建环境完成。验证的构建提交为 `4ebd79651283c6d7dcb3266f7af4b4291689a21e`，之后只补充发布说明。

[完整成功运行记录](https://github.com/ZCJ-GIF/QluCampus/actions/runs/35748739462)。所有测试均在后台云端运行，未占用用户电脑前台。

| 项目 | 结果 |
| --- | --- |
| Swift 业务单元测试 | 9 / 9 通过；学期映射、单双周/离散周、重复课表、日期时区、成绩排序/重名歧义、GPA 缺失与等价小数、锁定过滤、中文 XLSX、教室位掩码/页面结构 |
| 原生存储与会话测试 | 2 / 2 通过；账号切换使在途请求失效、账号缓存隔离、持久化后重读、无效写入不覆盖原课表、备份隐藏分项、按账号保留解锁状态 |
| iPhone 模拟器操作测试 | 1 / 1 通过；新增课程和地点、修改字体、成绩红绿标签、锁定后分项隐藏、全选 GPA=2.333、切换空教室与课表页面 |
| 真机架构 Release | arm64 iPhoneOS 无签名编译通过 |
| 实际安装包校验 | Info.plist 版本 0.1.0 (1)、最低系统 iOS 16、包标识和 arm64 可执行文件校验通过；持续集成已加入此检查，防止生成器写入默认版本号 |
| WidgetKit | 独立扩展编译通过；带 App Group 的应用与嵌入组件构建通过 |
| 视觉检查 | 已检查真实模拟器截图，日期同行工具栏、课程与地点、9–10 节、半透明导航均可见；截图使用虚构数据 |
| 测试数据隔离 | 演示数据和 `--uitest` 启动入口仅在 DEBUG 编译，Release 包不包含此入口 |

Swift Testing 的额外“0 tests”一行来自未使用的另一套测试框架；实际执行的是日志中列出的 9 个 XCTest 业务测试，不应把这行当作没有执行测试。

IPA、配套源码、SHA256、包标识/架构核验和日志保存在 `E:\Android\releases\ios-0.1.0`。配套源码带生成的 Xcode 工程和依赖锁文件，编译的 Swift 文件逐项与成功 CI 产物比较；发布说明补充不改变应用代码。

边界：真实学校登录和结果、iPhone aTrust 网络、设备通知送达、签名后的小组件添加仍待真机核验。首个 iOS 版本没有旧版覆盖升级验证。当前 IPA 没有 Apple 个人签名，需自行签名后安装；不是可直接点击安装的包。普通个人包不带小组件扩展，组件构建需 App Group 权限。Android 版本号、签名、数据库和更新源保持原状。
